#include<stdio.h>
float height,base;
float area()
{
    return(((0.5) * base * height));
}
int main(void)
{

  printf("Enter base and height:\n");
  scanf("%f%f",&base,&height);

  printf("Area:%f\n",area());
}
